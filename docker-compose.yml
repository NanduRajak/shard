services:
  inngest:
    image: inngest/inngest:v1.17.5
    container_name: shard-inngest
    command: inngest dev -u http://host.docker.internal:3000/api/inngest
    ports:
      - "8288:8288"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
